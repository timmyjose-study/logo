to acronym :name
  output reduce "word initials :name
end

to initials :names
  output map "first filter "non.connective :names
end

to non.connective :name
  output not memberp map "lowercase :name [if then else and of the of it is by at in on over]
end