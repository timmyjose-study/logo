to foreach.demo :things
  foreach :things "print
end

to foreach.demo.2
  foreach crossproduct [[ultra chocolate] pumpkin [root beer swirl] ginger] [cone cup] "print
end

to crossproduct :these :those
  output map.se [prepend.each :these ?] :those
end

to prepend.each :these :that
  output map [sentence ? :that] :these
end